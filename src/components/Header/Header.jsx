import { Title } from '../Title/Title'
import { Nav } from '../Nav/Nav'

const Header = () => {
  return (
    <header className="header">
      <Title />
      <Nav />
    </header>
  )
}

export { Header }
